<div class="container">
  <h2>Show</h2>
  <h3><%= @vote.title %></h3>
  <ul>
    <% @vote.vote_items.each do |vi| %>
      <li>
        <%= link_to vote_vote_path(vote_id: @vote, vote_item_id: vi.id), method: :post do %>
          <%= "#{vi.item} ( #{vi.vote_answers.count} )" %>
        <% end %>
      </li>
    <% end %>

    <%= pie_chart @vote %>
  </ul>


  <%= link_to "戻る", votes_path %>

</div>