<%= form_with model: comment, url: post_comment_path(params[:post_id], comment.id), local: true do |f| %>
  <label for= "inputName">Comment</label>
  <%= f.text_field :comment, id: "test" %>
  <%= f.submit "変更を保存" %>
<% end %>